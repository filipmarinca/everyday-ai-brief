{
  "name": "everyday-ai-brief",
  "version": "1.0.1",
  "private": true,
  "description": "A free, offline-capable educational prompt builder. No AI API or account.",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/brief.test.cjs tests/course-signal.test.mjs",
    "test:browser": "playwright test",
    "preview": "node scripts/serve.mjs"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
